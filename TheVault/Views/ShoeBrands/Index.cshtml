@model IEnumerable<TheVault.Models.ShoeBrand>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Brands)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Brands)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoeBrandID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoeBrandID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoeBrandID })
        </td>
    </tr>
}

</table>
